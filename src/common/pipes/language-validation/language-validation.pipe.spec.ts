import { LanguageValidationPipe } from './language-validation.pipe';

describe('LanguageValidationPipe', () => {
  it('should be defined', () => {
    expect(new LanguageValidationPipe()).toBeDefined();
  });
});
